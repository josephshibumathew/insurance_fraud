# Fixtures

This directory stores test fixtures and mock payloads.

- `sample_claim_data.json`: canonical claim payload for API and ML tests.
- `mock_groq_response.json`: deterministic LLM response for report generation tests.
