# Monitoring Stack

This folder provides baseline observability assets:

- `prometheus.yml`: API metrics scraping
- `grafana/fraud-dashboard.json`: dashboard starter panels
- `docker-compose.monitoring.yml`: Prometheus/Grafana/ELK local stack
- `sentry.md`: error tracking setup guide
